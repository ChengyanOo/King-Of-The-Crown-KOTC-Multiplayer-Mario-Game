/*using Microsoft.Xna.Framework;
using Microsoft.Xna.Framework.Graphics;
using Sprint1.Collisions;
using Sprint1.Scrolling;
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace Sprint1.Scrolling
{ /*
    internal class View
    {
        List<Layer> layers;
        private CollisionDetector collisionDetector;

        public View(List<Layer> layers)
        {
            this.layers = layers;
        }

        public void Draw(SpriteBatch spriteBatch)
        {
            foreach(var layer in layers)
            {
                layer.Draw(spriteBatch, collisionDetector.isColliderVisible);
            }
        }

        public void Update()
        {
            foreach (var layer in layers)
            {
                //layer.Update();
            }
        }

        public void ScrollRight(Vector2 cameraPosition)
        {
            foreach (var layer in layers)
            {
                layer.ScrollRight(cameraPosition);
            }
        }
    }
    
}
*/